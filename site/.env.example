# Optional. If set, "Open live demo →" button on landing page links here.
# Get this URL by running `cloudflared tunnel --url http://localhost:3000`
# on the machine running meme-weather.
NEXT_PUBLIC_LIVE_DEMO_URL=

# Optional. If set, "Open ZeroU dashboard →" button on the site links here.
# Get this URL by running `cloudflared tunnel --url http://localhost:5173`
# on the machine running ZeroU.
NEXT_PUBLIC_DASHBOARD_URL=
